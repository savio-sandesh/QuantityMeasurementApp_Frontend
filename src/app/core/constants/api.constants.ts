import { environment } from 'src/environments/environment';

export const AUTH_API_BASE_URL = `${environment.apiBaseUrl}/auth`;
export const QUANTITY_API_BASE_URL = `${environment.apiBaseUrl}/quantities`;
